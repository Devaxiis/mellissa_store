import 'package:flutter/material.dart';

class XodimlarScreen extends StatelessWidget {
  const XodimlarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Xodimlar Screen'),
      ),
    );
  }
}

