import 'package:flutter/material.dart';

class PulBirliklariScreen extends StatelessWidget {
  const PulBirliklariScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Pul Birliklar Screen'),
      ),
    );
  }
}
